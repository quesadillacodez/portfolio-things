## 2025-09-16 - Explicit Accessible Names for Search Triggers
**Learning:** Text-only or minimalist button triggers like `Search` benefit from an explicit `aria-label` (e.g., `aria-label="Search site"`) to provide complete context for assistive technologies across various screen readers.
**Action:** Always check interactive modal triggers for descriptive ARIA labels.
